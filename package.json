{
  "name": "kageyomi",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspace @kageyomi/agent && npm run build --workspace @kageyomi/web",
    "typecheck": "npm run typecheck --workspace @kageyomi/agent && npm run typecheck --workspace @kageyomi/web"
  },
  "devDependencies": {
    "@types/node": "^22.15.17",
    "ts-node": "^10.9.2",
    "tsx": "^4.19.4",
    "typescript": "^5.8.3"
  }
}
